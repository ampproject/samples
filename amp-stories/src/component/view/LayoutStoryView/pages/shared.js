import styled from 'styled-components';

import {TextHighlightBanner} from '/component/base/TextHighlight';

export const Text = styled(TextHighlightBanner).attrs({
  backgroundColor: 'storiesBlue',
  color: 'white',
})``;
