/**
 * Ideally this theme should be used to control much of the properties and
 * styling of the page. Unfortunately we didn't quite get around to using it
 * everywhere we should be. A straightforward exercise to replace hard-coded
 * colors with things like `${({theme}) => theme.colors.datPurp}` though,
 * should one be so adventerous.
 */
const theme = {
  colors: {
    datPurp: '#4C2F9B',
    heartOfDarkness: '#000',
    gloriousWhite: '#fff',
  },
};

export default theme;
